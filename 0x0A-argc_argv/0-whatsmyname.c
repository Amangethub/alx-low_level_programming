#include <stdio.h>

/**
 * main - prints out the function name
 *
 * @argc: argument counter
 * @argv: array of the argument strings
 *
 * Return: Always 0 (Successful)
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
